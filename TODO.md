# TODO - Dashboard chat UI updates

- [ ] Implement auto-scroll to bottom in `Frontend/src/features/chat/pages/Dashboard.jsx` when messages change.
- [ ] Improve AI message rendering for markdown + code blocks (fenced code, inline code) using `ReactMarkdown` component overrides.
- [ ] Update message bubble/layout styling for user vs AI.
- [ ] Add user short info at the bottom of the left chat history panel (in `Dashboard.jsx`).
- [ ] Run frontend build/dev server and manually verify:
  - [ ] sending prompt scrolls to bottom
  - [ ] AI responses render well for code and markdown
  - [ ] user info appears at bottom-left panel

